// Este archivo ha sido movido a layout-backup.tsx
// porque ahora usamos una página unificada sin pestañas laterales
export default function TourEditorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
